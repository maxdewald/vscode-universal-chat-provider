{
  "publisher": "maxdewald",
  "name": "universal-chat-provider",
  "displayName": "Universal Chat Provider",
  "type": "module",
  "version": "0.7.4",
  "packageManager": "pnpm@11.7.0",
  "volta": {
    "node": "24.17.0",
    "pnpm": "11.7.0"
  },
  "description": "Your AI subscriptions — Claude, ChatGPT/Codex, Gemini & more — in GitHub Copilot Chat. No API key, just OAuth.",
  "author": "Maximilian Dewald <maximilian@dewald.one>",
  "license": "MIT",
  "homepage": "https://github.com/maxdewald/vscode-universal-chat-provider#readme",
  "repository": {
    "type": "git",
    "url": "https://github.com/maxdewald/vscode-universal-chat-provider.git"
  },
  "bugs": {
    "url": "https://github.com/maxdewald/vscode-universal-chat-provider/issues"
  },
  "keywords": [
    "language-models"
  ],
  "categories": [
    "AI",
    "Chat"
  ],
  "main": "./dist/index.cjs",
  "icon": "res/logo.png",
  "files": [
    "LICENSE.md",
    "dist/*",
    "res/*"
  ],
  "engines": {
    "node": ">=22.18.0",
    "vscode": "^1.125.0"
  },
  "activationEvents": [
    "onStartupFinished"
  ],
  "contributes": {
    "languageModelChatProviders": [
      {
        "vendor": "universal-chat-provider",
        "displayName": "Universal Chat Provider"
      }
    ],
    "commands": [
      {
        "command": "universalChatProvider.manage",
        "title": "Universal Chat Provider: Manage Provider"
      },
      {
        "command": "universalChatProvider.login",
        "title": "Universal Chat Provider: Add Account (Login)"
      },
      {
        "command": "universalChatProvider.manageAccounts",
        "title": "Universal Chat Provider: Manage Accounts"
      },
      {
        "command": "universalChatProvider.restartServer",
        "title": "Universal Chat Provider: Restart Managed Server"
      },
      {
        "command": "universalChatProvider.updateBinary",
        "title": "Universal Chat Provider: Update Proxy Binary"
      },
      {
        "command": "universalChatProvider.resetServer",
        "title": "Universal Chat Provider: Reset Managed Server"
      },
      {
        "command": "universalChatProvider.configure",
        "title": "Universal Chat Provider: Configure Connection"
      },
      {
        "command": "universalChatProvider.importConfig",
        "title": "Universal Chat Provider: Import API Key from Config"
      },
      {
        "command": "universalChatProvider.refresh",
        "title": "Universal Chat Provider: Refresh Models"
      },
      {
        "command": "universalChatProvider.setUtilityModel",
        "title": "Universal Chat Provider: Set Utility Model (commit messages, chat titles, summaries)"
      },
      {
        "command": "universalChatProvider.clearCredentials",
        "title": "Universal Chat Provider: Clear Stored API Key"
      },
      {
        "command": "universalChatProvider.showLogs",
        "title": "Universal Chat Provider: Show Logs"
      },
      {
        "command": "universalChatProvider.showServerLogs",
        "title": "Universal Chat Provider: Show Server Output"
      },
      {
        "command": "universalChatProvider.openSettings",
        "title": "Universal Chat Provider: Open Settings"
      }
    ],
    "configuration": {
      "type": "object",
      "title": "Universal Chat Provider",
      "properties": {
        "universalChatProvider.server.mode": {
          "type": "string",
          "enum": [
            "managed",
            "external"
          ],
          "enumDescriptions": [
            "Download and supervise a bundled CLIProxyAPI server automatically.",
            "Connect to a CLIProxyAPI server you start yourself (uses Base URL and your own API key)."
          ],
          "default": "managed",
          "description": "How the CLIProxyAPI server is provided. 'managed' runs it for you; 'external' connects to your own instance."
        },
        "universalChatProvider.server.version": {
          "type": "string",
          "default": "7.2.5",
          "description": "CLIProxyAPI release to run in managed mode. Use a pinned version for reproducible installs, or 'latest' to track new releases."
        },
        "universalChatProvider.server.suggestUpdates": {
          "type": "boolean",
          "default": true,
          "description": "When a pinned managed server has a newer release within the same major version, offer to update to it. New major versions are never suggested automatically. No effect with 'latest' or in external mode."
        },
        "universalChatProvider.baseUrl": {
          "type": "string",
          "default": "http://127.0.0.1:8317",
          "description": "Base URL of the CLIProxyAPI server. Used only in external mode; the managed server picks its own port."
        },
        "universalChatProvider.configPath": {
          "type": "string",
          "default": "",
          "description": "Optional path to CLIProxyAPI config.yaml for credential and model metadata discovery."
        },
        "universalChatProvider.autoDetectConfig": {
          "type": "boolean",
          "default": true,
          "description": "Search common local CLIProxyAPI config locations when no config path is set."
        },
        "universalChatProvider.defaultMaxOutputTokens": {
          "type": "number",
          "default": 16384,
          "minimum": 1024,
          "description": "Fallback output-token limit when CLIProxyAPI provides no model-specific value."
        },
        "universalChatProvider.debug": {
          "type": "boolean",
          "default": false,
          "description": "Debug mode: show the live prompt-cache hit rate in the status bar and write per-request diagnostics to debug.jsonl in the extension's global storage — token usage (cache-read vs cache-write vs uncached, with a hit rate), the request message-prefix fingerprints, and the cross-turn prefix diff (what changed since the previous turn, the thing that breaks the cache). Off by default; use it to diagnose caching and rate-limit issues."
        }
      }
    }
  },
  "scripts": {
    "build": "tsdown",
    "dev": "tsdown --watch --sourcemap",
    "prepare": "pnpm generate",
    "generate": "vscode-ext-gen --output src/generated/meta.ts",
    "lint": "eslint .",
    "test": "vitest run --project test:unit",
    "test:e2e": "vitest run --project test:e2e",
    "test:unit": "vitest run --project test:unit",
    "test:coverage": "vitest run --project test:unit --coverage",
    "test:watch": "vitest --project test:unit",
    "typecheck": "tsc --noEmit",
    "check": "pnpm lint && pnpm typecheck && pnpm test:coverage && pnpm build",
    "vscode:prepublish": "pnpm build",
    "ext:package": "vsce package --no-dependencies",
    "ext:publish": "vsce publish --no-dependencies"
  },
  "dependencies": {
    "eventsource-parser": "^3.1.0",
    "fflate": "^0.8.3",
    "get-port": "^7.2.0",
    "moderndash": "^4.0.2",
    "nanotar": "^0.3.0",
    "semver": "^7.8.4",
    "tokenx": "^1.3.0",
    "yaml": "^2.9.0"
  },
  "devDependencies": {
    "@antfu/eslint-config": "^9.0.0",
    "@types/node": "^24.13.2",
    "@types/semver": "^7.7.1",
    "@types/vscode": "^1.125.0",
    "@vitest/coverage-v8": "^4.1.8",
    "@vscode/vsce": "^3.9.2",
    "eslint": "^10.5.0",
    "tsdown": "^0.22.2",
    "typescript": "^6.0.3",
    "vite": "^8.0.16",
    "vitest": "^4.1.8",
    "vscode-ext-gen": "^1.6.0"
  }
}
